import minesweeper_settings

def height_prct(percentage):
    return (minesweeper_settings.HEIGHT / 100) * percentage
    
def width_prct(percentage):
    return (minesweeper_settings.WIDTH / 100) * percentage
    